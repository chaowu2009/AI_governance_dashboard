# Copy this file to .env and fill in real values.
# .env is git-ignored by default.

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
CAPTCHA_SECRET=replace-with-random-hex

# Governance reviewer token used by moderation endpoints and UI forms.
GOVERNANCE_TOKEN=replace-with-strong-reviewer-token
